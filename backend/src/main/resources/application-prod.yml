spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update # 나중에 none으로 변경
    properties:
      hibernate:
        format_sql: true
